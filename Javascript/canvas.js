

/* função drawMenu precisa ser ativada antes do jogo */

/* document.addEventListener('click', mouseClicked, false )
 */
/* let btnplay = new Button(265,535,220,335);
 */
/* button object */
/* function Button(xl,xr,yt,yb) {
    this.xLeft = xl;
    this.xRight = xr;
    this.yTop = yt;
    this.yBottom = yb;
} */

/* Button.prototype.checkClicked = function(){
/* se o botão for clicado na área, retorne true */
/*  if(this.xLeft <= mouseX && mouseX <= this.xRight && this.yTop <= mouseY && mouseY <= this.yTop) return true;
}; */

/* 
let mouseX = 0;
let mouseY = 0;
 */

/* Event functions
 */

/* function mouseClicked (e) {
    mouseX = e.pageX - canvasBg.offsetLeft;
    mouseY = e.pageY - canvasBg.offsetTop;
    if (btnplay.checkClicked()) startGame();
} */
